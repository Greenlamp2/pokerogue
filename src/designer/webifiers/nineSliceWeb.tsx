import React from "react";

const NineSliceWeb = (element) => {
  console.log("element", element);
  const leftWidth = element.leftWidth;
  const rightWidth = element.rightWidth;
  const topHeight = element.topHeight;
  const bottomHeight = element.bottomHeight;
  const width = element.width;
  const height = element.height;
  const texture = element.texture;

  const ret = (
    <div>OK</div>
  );

  return ret;
};

export default NineSliceWeb;
